$ argus lock revoke-signer --finish <BLOB-COSIGNED> --socket=<node-a-SOCKET> --gateway=ws://<GW-ADDR> --token=<TOKEN>
exit 0
--- stdout ---
revocation applied
  new tip (audit): <TIP-1>
--- stderr ---

Revocation propagates to the network within ~<DUR>.
