$ argus lock pin <GENESIS> --socket=<node-b-SOCKET> --gateway=ws://<GW-ADDR> --token=<TOKEN>
exit 0
--- stdout ---
pinned this device
  genesis: <GENESIS>
  <FP>
  local node pinned and enforcing
--- stderr ---
